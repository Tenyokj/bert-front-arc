# Errors

**Summary**
Centralized error definitions for the DAO system.

**Role In System**
Standardized revert reasons shared across all contracts for consistent debugging.

**Key Features**
- Validation errors (zero address, invalid id)
- Role and permission errors
- State errors (already distributed, not ended, etc.)
